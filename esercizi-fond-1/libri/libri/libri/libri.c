#include "libri.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct libro* read_libri(const char* filename, unsigned int* pn) {

	struct libro* plibro = NULL;
	size_t n = 0;

}


	/*contatore++;
	realloc(p, contatore);
	fscanf(filename, "%i%s%i", &datiLibro.codice, &datiLibro.titolo, &datiLibro.pagine);
	p[contatore] = datiLibro;*/